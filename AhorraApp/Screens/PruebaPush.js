import { Text, StyleSheet, View } from 'react-native'
import React, { Component } from 'react'

export default class PruebaPush extends Component {
  render() {
    return (
      <View>
        <Text>PruebaPush</Text>
      </View>
    )
  }
}

const styles = StyleSheet.create({})